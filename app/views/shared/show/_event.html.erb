<div class="box box-info">
  <div class="box-header">
    <i class="fa fa-location-arrow"></i>
    <h3 class="box-title"><%= @event.name %></h3>
  </div>
  <div class="box-body">
    <div class="row">
      <div class="text-center">
        <div class="description-block border-right">
          <h5 class="description-header"><i class="text-blue fa fa-calendar"></i> Data</h5>
          <% if @event.date? %>
            <span class="description-text"><%= @event.date.strftime("%d/%m/%Y") %></span>
          <% end %>
        </div>
        <!-- /.description-block -->
      </div>
    </div>
    <div class="callout bg-light-blue-active">
      <h4>Sobre:</h4>
      <p><%= @event.description %></p>
    </div>
  </div>
</div>

<div class="box box-danger">
  <div class="box-header with-border">
    <h3 class="box-title">Participantes</h3>
    <div class="box-tools pull-right">
      <button type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
    </div>
  </div>
  <!-- /.box-header -->
  <div class="box-body">
    <ul class="users-list clearfix">
      <% @event_users.each do |user| %>
        <li>
          <% #user = User.find(pt.user_id)  %>
          <% if user.photo? %>
            <%= image_tag(user.photo.profile.url, :class => "img-circle img-list-event") %>
          <% else %>
            <img src="/images/gti_user_pic.png" class="img-list-event" alt="User Image" />
          <% end %>
          <% if controller_path.include? 'administrative' %>
            <%= link_to "#{user.name}", administrative_user_path(user), class:"users-list-name" %>
          <% elsif controller_path.include? 'dashboard' %>
            <%= link_to "#{user.name}", dashboard_user_path(user), class:"users-list-name" %>
          <% end %>
          <span class="users-list-date"><%= user.role %></span>
        </li>
      <% end %>
    </ul>
    <!-- /.users-list -->
  </div>
</div>