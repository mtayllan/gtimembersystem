<%= form_tag dashboard_users_path, method: :get do %>
  <div class="box box-primary">
    <div class="box-header with-border">
      <h3 class="box-title">Usuários</h3>
      <div class="box-tools">
        <div class="input-group input-group-sm" style="width: 150px;">
          <%= text_field_tag(:q, params[:q] , { class:"form-control pull-right", placeholder: "Pesquisar" }) %>
          <div class="input-group-btn">
            <%= button_tag(type: "submit", class: "btn btn-default") do %>
                <i class="fa fa-search"></i>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <!-- /.box-header -->
    <div class="box-body" style="">
      <table class="table table-hover">
        <tbody><tr>
          <th>Foto</th>
          <th>Nome</th>
          <th>Cargo</th>
          <th></th>
          </tr>
        <% @users.each do |user| %>
          <tr>
            <td> 
              <% if user.photo? %> 
                <%= image_tag(user.photo.sidebar.url, :class => "img-responsive") %>
              <% else %>
                <img src="/images/gti_user_pic50p.png" class="img-responsive" alt="User Image" />
              <% end %>
            </td>
            <td><%= user.name %></td>
            <td><%= user.role %></td>
            <td>
              <%= link_to dashboard_user_path(user), class: "btn btn-sm btn-info" do %>
                <i class="fa fa-eye"></i>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <hr>
    <%= paginate @users %>
    </div>
  </div> 
<% end %>

